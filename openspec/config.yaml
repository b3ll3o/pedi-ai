schema: spec-driven

context: |
  Tech stack: Next.js + TypeScript, Supabase (Backend as a Service), Service Workers, IndexedDB
  Architecture: Single Next.js PWA application with integrated E2E tests
  Testing: Vitest (unit), Playwright + Cucumber (E2E), 80%+ coverage threshold
  Style: TBD (no convention files detected in empty project)

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules/packages
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (infrastructure, implementation, testing)
    - Use hierarchical numbering (1.1, 1.2, etc.)
    - Keep tasks small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions
    tdd: false
    test_command: ''
  verify:
    test_command: ''
    build_command: ''
    # Meta de coverage: 80% para novo código, 50% para código existente (migração gradual)
    coverage_threshold: 50
  archive:
    - Warn before merging destructive deltas
